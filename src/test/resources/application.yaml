spring:
  application:
    name: unit-test
  datasource:
    driverClassName: org.h2.Driver
    jdbc-url: jdbc:h2:mem:demo-test
    auto-commit: false
  h2:
    console:
      enable: true
      setting:
        web-allow-others: true